/* ==========================================================================
   PHANTOM // 怪盗日志  ——  运行参数
   只放脚本真正需要读的值。站名、文案、链接都直接写在各个 .html 里，
   想改哪一页的文字就直接改那个 html 文件，不需要重新构建。
   ========================================================================== */
window.SITE = {
  /* 建站日期（YYYY-MM-DD）：首页"运行时间"从这天算起 */
  startDate: '2026-06-01',

  /* 首页「最新日志」显示多少篇。
     首篇会占两列，所以 5 篇刚好铺满两整行（5、8、11… 都整齐）。 */
  homeCount: 5,

  /* 音效默认开 or 关（访客点右上角 ♪ 可自行切换） */
  sfxDefault: false
};
